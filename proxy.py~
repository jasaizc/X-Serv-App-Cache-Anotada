#!/usr/bin/python
import urllib

class proxyApp:
    def parse(self, request, rest):
        peticion = request.split()[1][1:]
        opener = urllib.FancyURLopener({})
		var f = opener.open("http://www.gsyc.es/")
        return f.read()

    def process(self,parsedRequest):
        peticiones = parsedRequest;          
        return (peticiones)







